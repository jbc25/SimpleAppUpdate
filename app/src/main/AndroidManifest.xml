<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/simple_app_update_corners_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/simple_app_update_round_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.SimpleAppUpdate"
        tools:targetApi="31"
        android:name=".App">
        <activity
            android:name="com.triskelapps.updateappviewsample.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>